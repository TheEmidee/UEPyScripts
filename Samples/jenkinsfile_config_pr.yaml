name: MyPipeline
features:
  properties:
    items:
      - "buildDiscarder( logRotator( numToKeepStr: '10' ) )"
      - "pipelineTriggers( [ cron( 'H 3 * * 1-6' ) ] )"
  slack_notifications:
    channel: "#ci-cd"
    //   use_simple_message: true
    //   channel_override: "#sdsdf"
    on_success:
      blocks_message:
        enabled: true
    // on_failure:
    //   use_simple_message: true
    // on_unstable:
    //   use_simple_message: true
    // on_exception:
    //   use_simple_message: true
  git:
    use_simple_checkout: false
    branch_name: "refs/heads/develop"
    extensions:
      CheckoutOption:
        timeout: 120
      SubmoduleOption:
        disableSubmodules: false
        parentCredentials: true
        recursiveSubmodules: true
        reference: ""
        timeout: 120
        trackingSubmodules: false
      GitLFSPull:
        enabled: true
    user_remote_config:
      credentials_id:
        id: "CredentialId"
        url: "git@github.com:Github/Octocat.git"
